node_modules/
dist/
.claude/
*.log

# Repo-root scratch screenshots from manual verification runs only —
# anchored to root so it doesn't also catch public/screenshots/**/*.png,
# which are real project assets the site serves.
/*.png
verify-shots/

.vercel
